# Critique Document

- Agent, model, and opencode version: opencode 1.18.32 (`opencode --version`); model `opencode/big-pickle` (this session's environment); agent profile `builder` (primary, `opencode.json`); runtime Python 3.13.0
- Candidate scored: Candidate 0 - commit `bcd7fde` "Candidate 0: agent's first implementation" (current HEAD; scored as the working tree, no revisions applied)
- Reviewer and date: opencode/big-pickle, 2026-09-24
- Weighted score: 0.97 against a threshold of 0.80
- I will accept this when: the weighted score is >= 0.80 and no Material criterion is rated "Does not meet" (the two acceptance rules in `rubric.json`); both hold, so Candidate 0 is accepted at 0.97.
- The one criterion that blocks acceptance outright: TC-06 - the only Material criterion not rated "Meets" (rated Approaching); resolve it and the acceptance is clean.

| ID | Criterion | Level | Material | Evidence (what you ran, what you saw) |
|----|-----------|-------|----------|---------------------------------------|
| TC-01 | Identical snapshots -> no changes | Meets | Yes | `python3 -m unittest discover -s artifact/tests`: test_identical_snapshots_no_changes green (exit 0, "No changes detected.", no "## "). CLI smoke of the real capture against itself: exit 0, header + "Summary: 0 changed assignments, 0 removed, 0 added." + "No changes detected." |
| TC-02 | Single field (points) change | Meets | Yes | test_single_points_change green. Manual run base.html -> points_changed.html: exit 0, "## Alpha", "- Points: 10 -> 30", no "Due date" line, no "## Beta". |
| TC-03 | Removal and addition | Meets | Yes | test_removal_and_addition green: "## Beta" + "- Assignment removed", "## Delta" + "- Assignment added", Beta section asserted before Delta. |
| TC-04 | Missing input -> exit 2, usage, no report | Meets | Yes | test_missing_input_exits_2_and_writes_nothing green in both arg orders: returncode 2, "usage" in stderr, out.md never created. |
| TC-05 | Unparseable snapshot -> exit 1, names file, no partial report | Meets | Yes | test_malformed_snapshot_exits_1_and_names_file green in both arg orders: returncode 1, "malformed" on stderr, out.md absent. |
| TC-06 | All criteria automated via the unittest command | Approaching | Yes | `python3 -m unittest discover -s artifact/tests`: 13 tests, OK, no skips. The automated suite does NOT exercise several verification criteria - TC-08 (header line), TC-14 (default -o path), TC-16 (stray-file scan), TC-17 (overwrite), TC-20 (offline) were verified manually instead - and two real-capture cases skip when `snapshots/` is absent (gitignored, so a fresh clone loses them). |
| TC-07 | All four supported change kinds surface | Meets | Yes | Points (TC-02), due-date ("- Due date: Sep 24 at 11:59pm -> not shown" in test_due_date_not_shown_transition), removed, added (TC-03) each surfaced in green tests; no kind silently skipped. Note: a two-real-dates due change is not fixture-exercised, but the same `old.due != new.due` branch covers it. |
| TC-08 | Header line names both snapshots + timestamp | Meets | Yes | Manual CLI run: first line "Canvas Snapshot Diff: <old> -> <new> at 2026-09-24 12:11:26" (build_report, canvas_diff.py:158-161). Not covered by the unittest suite. |
| TC-09 | One-line summary N changed / M removed / K added | Meets | Yes | Tests assert "Summary: 1 changed assignments, 0 removed, 0 added." and "Summary: 0 changed assignments, 1 removed, 1 added."; manual runs matched those numbers. |
| TC-10 | `## <Title>` sections with documented bullet labels | Meets | Yes | Tests assert "## Alpha", "- Points: 10 -> 30", "- Due date: Sep 24 at 11:59pm -> not shown", "- Assignment removed", "- Assignment added"; "Description edited" is never produced. |
| TC-11 | Per-assignment fields extracted, pinned vs real capture, To-Do only | Meets | Yes | test_real_capture_maps_to_expected_records RAN (capture present, not skipped): exactly 3 titles (Lab: OpenCode Studio, Homework 3, Writing as Thinking - Week 5) with correct due/points incl. "not shown". Parser unit test pins title (aria-label), points ("N points"), due (date li); Announcement/Calendar-Event items ignored; duplicate titles dedupe first-wins; unresolvable record raises -> exit 1. Skips on a fresh clone. |
| TC-12 | Exit codes 0/1/2; open-failure 2 vs parse-failure 1 | Meets | Yes | 0 (identical), 1 (malformed), 2 (missing input, unwritable OUT_MD) all green via tests; my chmod-000 input ran -> exit 2, usage on stderr, no output file. Open and parse failures are not conflated. |
| TC-13 | CLI shape and order; one HTML file per snapshot | Meets | Yes | Ran `python3 artifact/canvas_diff.py OLD_HTML NEW_HTML [-o OUT_MD]` exactly as documented, with and without `-o`. OLD->NEW direction proven: base(OLD) has Beta removed, new side Delta added. |
| TC-14 | Default output is artifact/report.md | Meets | No | Ran without `-o` on the real capture: report written to artifact/report.md (verified then removed). Not covered by the suite. |
| TC-15 | Python 3, standard library only (html.parser/argparse/unittest) | Meets | Yes | Imports in canvas_diff.py: argparse, datetime, html.parser, os, re, sys, dataclasses - all stdlib. `python3 -m py_compile artifact/canvas_diff.py` OK. Tests use unittest. |
| TC-16 | Exactly one .md file at OUT_MD; nothing else written | Meets | Yes | Temp-dir run left exactly base.html, new.html, out.md - one .md, no temp/backup files present after the run. |
| TC-17 | OUT_MD overwritten in place each run | Meets | No | Two runs to the same out.md: single file remains, content is run 2 ("0 changed, 1 removed, 1 added", Beta/Delta sections), no * _old/_new/backup copies. Not covered by the suite. |
| TC-18 | Regression test per bug fix | Meets (vacuous) | No | `git log` (10 commits) shows no bug-fix commits to date - HEAD bcd7fde is the first implementation itself - so there is no fix to rate; flagged as vacuous. |
| TC-19 | Sections sorted alphabetically by title | Meets | No | `sorted(...)` in build_report (canvas_diff.py:167); test asserts Beta section before Delta; all manual report outputs were alphabetized. |
| TC-20 | Fully offline; no network access | Meets | Yes | grep across artifact/ for urlopen/http/socket/requests: no matches (only rubric.md's own text); canvas_diff.py reads only local paths; no network code path. Host networking was not literally disabled. |
| PR-01 | Never modify/delete/overwrite snapshot inputs | Meets | Yes | test_snapshot_is_never_modified green; shasum of snapshots/Canvas_y26M09d23h21m04s59.html identical before and after a CLI run (503cfe1c...); inputs opened with mode "r" only. |
| PR-02 | Never touch transcripts/ | Meets | Yes | `git status --short transcripts/` empty; both tracked transcripts files (03-rule-alone.md, 04-gate-held.md) unchanged. |
| PR-03 | CHARTER.md/AGENTS.md/spec.md/.ai/(docs/) read-only unless asked | Approaching | No | MEMORY.md diff is a single dated bottom append (OK). ADJUDICATION REQUIRED: `.ai/SESSION.md` is an untracked new file under `.ai/` that is not the MEMORY append; whether its creation violated the Ask gate is not established by project rules today - recorded as requiring adjudication, not a definite violation. (Also `artifact/__pycache__/*.pyc` is tracked in git and shows modified - repo hygiene, not a tool write.) |
| PR-04 | opencode.json, README.md, START_HERE.md unchanged | Meets | No | `git status` shows none of these modified. |
| PR-05 | Writes only OUT_MD, including on error | Meets | Yes | Error runs (missing input, malformed fixture, chmod-000 input) produced no output file (tests assert absence; perms-denied run confirmed); success run in a clean dir wrote only out.md. |
| PR-06 | MEMORY.md changes limited to dated bottom appends | Meets | No | `git diff .ai/MEMORY.md`: exactly one dated 2026-09-24 entry appended at the bottom; nothing rewritten or deleted. |
| PR-07 | No code comments unless asked | Meets | No | `grep '^\s*#'` across artifact/*.py and artifact/tests/*.py: only the line-1 shebang `#!/usr/bin/env python3`; no prose comments. |
| PR-08 | No secrets; credential failure stops and reports once | Meets | Yes | No token/secret/api-key/password patterns in artifact/ code or docs; `git log -p` contains no `sk-` values; the tool is offline so the credential-failure scenario is vacuous. |
| PR-09 | sources/ and snapshots/ immutable | Meets | Yes | sources/ absent; snapshots/ holds one capture whose shasum is unchanged across runs; test asserts no write; `git status` clean. |